*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
/build
/app/build
/captures
.externalNativeBuild
.cxx
local.properties
*.hprof
.kotlin/

# Release signing. NEVER commit these — losing keystore.properties is recoverable
# (regenerate it), losing the keystore file itself is not (see PLAY_STORE_LISTING.md).
keystore.properties
*.jks
*.keystore

# Built APKs staged for a GitHub release. Artefacts, not source.
dist/

# Firebase CLI deploy cache — local build state, not source.
.firebase/
